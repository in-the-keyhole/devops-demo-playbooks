apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: {{ app.name }}
  name: {{ app.name }}
spec:
  port:
    targetPort: "{{ app.port | default('8080')}}-tcp"
  to:
    kind: Service
    name: {{ app.name }}
    weight: 100
  wildcardPolicy: None